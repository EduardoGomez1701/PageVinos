const MenuV = document.getElementById('MenuV');

// Agrega un evento de clic al botón
MenuV.addEventListener('click', () => {
    // Redirige a la página MenuVinios.html
    window.location.href = 'MenuVinos.html';
});

const MenuW = document.getElementById('MenuW');

// Agrega un evento de clic al botón
MenuW.addEventListener('click', () => {
    // Redirige a la página MenuVinios.html
    window.location.href = 'MenuWhysky.html';
});